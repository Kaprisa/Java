package graphics;

import java.awt.*;

public class GraphicFunction {
    private Color color;

    public GraphicFunction(String color) {
        this.color = Color.getColor(color);
    }
}
